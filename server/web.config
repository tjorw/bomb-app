<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.webServer>

    <!-- Stäng av WebDAV som ger 405 på DELETE/PUT -->
    <modules runAllManagedModulesForAllRequests="true">
      <remove name="WebDAVModule" />
    </modules>
    <handlers>
      <remove name="WebDAV" />
      <remove name="WebDAVClassic" />
      <add name="aspNetCore" path="*" verb="*" modules="AspNetCoreModuleV2" resourceType="Unspecified" />
    </handlers>

    <rewrite>
      <rules>
        <!-- SPA fallback: bara GET, undanta API/Hub/Assets/verkliga filer -->
        <rule name="ReactRouterFallback" stopProcessing="true">
          <match url=".*" />
          <conditions logicalGrouping="MatchAll">
            <add input="{REQUEST_METHOD}" pattern="^GET$" />
            <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />
            <add input="{REQUEST_FILENAME}" matchType="IsDirectory" negate="true" />
            <!-- OBS: REQUEST_PATH (utan query) -->
            <add input="{REQUEST_PATH}" pattern="^/api($|/)" negate="true" />
            <add input="{REQUEST_PATH}" pattern="^/bomhub($|/)" negate="true" />
            <add input="{REQUEST_PATH}" pattern="^/bomhub/negotiate$" negate="true" />
            <!-- Undanta statiska resurser -->
            <add input="{REQUEST_PATH}" pattern="^/assets/" negate="true" />
            <add input="{REQUEST_PATH}" pattern="\.(js|css|map|ico|png|jpg|svg)$" negate="true" />
          </conditions>
          <action type="Rewrite" url="/index.html" />
        </rule>
      </rules>
    </rewrite>

    <!-- (valfritt men ibland behövs) tillåt fler verb -->
    <security>
      <requestFiltering allowDoubleEscaping="true">
        <verbs allowUnlisted="true">
          <add verb="DELETE" allowed="true" />
          <add verb="PUT" allowed="true" />
          <add verb="OPTIONS" allowed="true" />
        </verbs>
      </requestFiltering>
    </security>

    <aspNetCore processPath="dotnet" arguments="server.dll" hostingModel="InProcess" />
  </system.webServer>
</configuration>
